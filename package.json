{
  "name": "portfolio-starter",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal portfolio starter (TypeScript + static build)",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "watch": "tsc --project tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.2.0"
  }
}
